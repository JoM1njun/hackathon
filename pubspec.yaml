name: dtrip
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  kakao_map_plugin: ^0.3.7
  webview_flutter: ^4.13.0
  geolocator: ^14.0.2
  flutter_inappwebview: ^6.1.5
  flutter_compass: ^0.8.1
  sliding_up_panel: ^2.0.0+1
  flutter_svg: ^2.2.2
  xml2json: ^6.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Location/gps_on.svg
    - assets/Location/gps_off.svg
    - assets/Places/

